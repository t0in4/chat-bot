# EMBEDDED H2 (no TCP server needed)
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.datasource.db-kind=h2
quarkus.datasource.username=sa
quarkus.datasource.password=
quarkus.datasource.jdbc.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1

# H2 Console
quarkus.h2-console.enable=true
quarkus.langchain4j.tools.enabled=true

quarkus.hibernate-orm.log.sql=true
quarkus.langchain4j.log-response=true
quarkus.redis.hosts=redis://localhost:6379
quarkus.http.auth.permission.authenticated.paths=/ride/chat/*
quarkus.http.auth.permission.authenticated.policy=authenticated
quarkus.oidc.enabled=false  # If not using OIDC
# GigaChat timeout
quarkus.langchain4j.gigachat.timeout=30s

quarkus.langchain4j.redis.dimension=768
quarkus.langchain4j.redis.hosts=redis://localhost:6379

# Reranking model path for BAAI/bge-reranker-large
embedding.model.path=protected

# Explicitly set OpenAI as default provider (satisfies BeansProcessor)
quarkus.langchain4j.chat-model.default-provider=openai

# Dummy OpenAI config (no real calls)
quarkus.langchain4j.openai.chat-model.model-id=gpt-4o-mini
quarkus.langchain4j.openai.embedding-model.model-id=text-embedding-3-small

# Your GigaChat named config overrides for injection
quarkus.langchain4j.gigachat.chat-model."giga".name=giga
quarkus.langchain4j.embedding-store.in-memory.enabled=true
quarkus.langchain4j.embedding-model.default-provider=local-embed

# LangChain4j Timeout (seconds)
quarkus.langchain4j.chat-model.timeout=30000

# Ensure Vertx HTTP client also respects timeouts

quarkus.log.category."org.jboss.resteasy.reactive.client.logging".level=DEBUG
quarkus.log.category."ai.djl".level=DEBUG



# --- GigaChat Configuration ---
quarkus.langchain4j.gigachat.base-url=https://gigachat.developer.sberbank.ru/api/v2/chat/completions
# Ensure the env variable GIGACHAT_API_KEY is set in your shell or .env file
quarkus.langchain4j.gigachat.api-key=${GIGACHAT_API_KEY}

# --- Timeout Configuration (CRITICAL FIX) ---
# LangChain4j uses the Vert.x HTTP client. These are the ONLY timeouts that work reliably
# without specific provider extensions supporting custom timeout keys.
# Set read timeout to 30 seconds (30000 ms)
quarkus.vertx.max-event-loop-execute-time=30S
quarkus.http.idle-timeout=30S

# Optional: Enable logging to see the request/response (helps debug if it hangs)
quarkus.langchain4j.log-requests=true
quarkus.langchain4j.log-responses=true

# --- Cleanup: Remove these lines as they are ignored/causing warnings ---
# quarkus.langchain4j.chat-model.timeout (IGNORED)
# quarkus.langchain4j.gigachat.timeout (IGNORED)
# quarkus.rest-client.read-timeout (IGNORED unless specific rest-client config is active for LC4j)